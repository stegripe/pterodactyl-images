name: Build & Push Node.js 16 to OCI Image Registry

on:
  push:
    branches:
      - main
    paths:
      - "node/16/**/Dockerfile"
  pull_request:
    branches:
      - main
    paths:
      - "node/16/**/Dockerfile"

jobs:
  alpine:
    name: Node.js 16 Alpine
    uses: ./.github/workflows/build.yml
    with:
      rootdir: node
      workdir: node/16/alpine
      image-name: node
    secrets:
      github-username: ${{ github.repository_owner }}
      github-token: ${{ secrets.GITHUB_TOKEN }}

  debian:
    name: Node.js 16 Debian
    uses: ./.github/workflows/build.yml
    with:
      rootdir: node
      workdir: node/16/debian
      image-name: node
    secrets:
      github-username: ${{ github.repository_owner }}
      github-token: ${{ secrets.GITHUB_TOKEN }}
